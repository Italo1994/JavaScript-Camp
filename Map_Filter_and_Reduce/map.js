// Utilizando o Map
const arr = [1, 2, 3, 4, 5];

console.log( arr.map((item) => item * 2) );

// Utilizando o forEach
console.log( arr.forEach((item) => item * 2)); // retorna undefined -> necessário uma constante para receber o retorno